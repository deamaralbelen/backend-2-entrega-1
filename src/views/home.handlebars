<h1>{{titulo}}</h1>
<p> ¡Bienvenido/a a la tienda! 🎉 </p>

<!-- Formulario para filtros y orden -->

<form method="GET" action="/">
  <label for="query"> Categoría / filtro: </label>
  <select name="query" id="query">
    <option value="" {{#unless query}}selected{{/unless}}> Todos </option>
    <option value="nike" {{#if (eq query "nike")}}selected{{/if}}> Nike </option>
    <option value="adidas" {{#if (eq query "adidas")}}selected{{/if}}> Adidas </option>
    <option value="puma" {{#if (eq query "puma")}}selected{{/if}}> Puma </option>
  </select>

  <label for="sort"> Ordenar por precio: </label>
  <select name="sort" id="sort">
    <option value="" {{#unless sort}}selected{{/unless}}> Sin orden </option>
    <option value="asc" {{#if (eq sort "asc")}}selected{{/if}}> Ascendente </option>
    <option value="desc" {{#if (eq sort "desc")}}selected{{/if}}> Descendente </option>
  </select>

  <button type="submit"> Filtrar </button>
</form>

<h2> Productos disponibles: </h2>
<ul>
  {{#each productos}}
    <li>
      <strong>{{title}}</strong> - ${{price}}<br>
      {{description}}<br>
      <em> Categoría: {{category}} </em>
    </li>
  {{/each}}
</ul>

<!-- Paginación -->

<nav>
  {{#if hasPrevPage}}
    <a href="{{prevLink}}"> « Página anterior </a>
  {{else}}
    <span> « Página anterior </span>
  {{/if}}

  <span> Página {{page}} de {{totalPages}} </span>

  {{#if hasNextPage}}
    <a href="{{nextLink}}"> Página siguiente » </a>
  {{else}}
    <span> Página siguiente » </span>
  {{/if}}
</nav>